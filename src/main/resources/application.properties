spring.application.name=MobileStoreR2S
## Database Properties
spring.datasource.url = jdbc:sqlserver://localhost:1434;encrypt=false;databaseName=MobileStoreR2S
spring.datasource.username = sa
spring.datasource.password = Khanhtrinh2610#

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.SQLServerDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

## Fix the dependencies of some of the beans in the application context form a cycle
spring.main.allow-circular-references = true

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

storage.location=D:/R2S/mock/images